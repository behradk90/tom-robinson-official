import type { Metadata } from "next";
import "./globals.css";
import Header from "@/Components/Layout/Header";
import SideBar from "@/Components/Layout/SideBar";
import { NavPreviewProvider } from "@/context/NavPreviewContext";
import Blurb from "@/Components/Layout/Blurb";

export const metadata: Metadata = {
  title: "The Tom Robinson Site",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="antialiased h-screen overflow-hidden bg-indigo-400">
        <NavPreviewProvider>
          <div className="h-full flex justify-center bg-indigo-400">
            <div className="flex h-full flex-col w-full max-w-6xl p-3">
              <Header />

              {/* Sidebar + Main row */}
              <div className="flex flex-1 overflow-hidden min-h-0 border-b-8">
                <SideBar />
                <div className="my-2 md:m-2 md:mr-0 w-full flex flex-col min-h-0 space-y-2">
                  <Blurb />
                  <main className="flex-1 overflow-y-auto">{children}</main>
                </div>
              </div>
            </div>
          </div>
        </NavPreviewProvider>
      </body>
    </html>
  );
}
